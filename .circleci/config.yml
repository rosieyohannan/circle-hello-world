version: 2.1

jobs:
  daily-message:
    docker:
      - image: cimg/base:2025.09
    steps:
      - run: echo "Hello"
      #- run: echo <<pipeline.event.github.pull_request.base.ref>>
      - run: echo <<pipeline.event.name>>

workflows:
  my-workflow:
    when: <<pipeline.event.name>> == "pull_request"
    jobs:
      - daily-message

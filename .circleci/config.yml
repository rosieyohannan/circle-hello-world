version: 2.1

jobs:
  daily-message:
    docker:
      - image: cimg/base:2025.09
    steps:
      - run: echo "Hello"
      - run: echo <<pipeline.event.action>>
      - run: echo <<pipeline.event.name>>

workflows:
  my-workflow:
    jobs:
      - daily-message

version: 2.1

jobs:
  daily-message:
    docker:
      - image: cimg/base:2025.09
    steps:
      - run: echo "Hello"

workflows:
  my-workflow:
    when: <<pipeline.event.github.pull_request.base.ref>>
    jobs:
      - daily-message
